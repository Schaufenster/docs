---
title: "egora data (live business data)"
description: "Connect Claude to the eGora data server to query orders, requests, companies, and KPIs directly in chat."
icon: "database"
---

The **egora-data MCP** connector gives Claude read access to live eGora business data — with PII redaction —
that is, orders, catering requests, companies, KPIs, and more.

<Note>
  This is **different** from the KB MCP. The KB MCP delivers *knowledge* (how things work); egora-data
  delivers *current data* (the actual numbers).
</Note>

## Setup

The full step-by-step guide (Claude Desktop, tokens, staging vs. production, troubleshooting) lives in the
IT guide:

<Card title="Connect Claude to eGora data (MCP)" icon="plug" href="/en/it/mcp-setup">
  In Claude web (Cowork / Teams): add a custom connector → `https://egora-catering.de/mcp` → leave client
  ID/secret blank → connect → authorize in Nova. (Token method available for Claude Code / Desktop.)
</Card>

## When to use it

Use egora-data when the answer depends on **current numbers**, e.g.:

- "How many B2B catering requests in the last 7 days, by city?"
- "What is our conversion rate this month?" (see also the [KPI catalog](/en/it/kpi-catalog))

For "how does X work" questions, use the KB instead.
